import { TrackByIdPipe } from './track-by-id-pipe';

describe('TrackByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
